"""Representations of saved runs: archive access and explicit rendering.

``tropoi.representation.archive`` (import-light) opens run capsules;
``tropoi.representation.visual`` (heavy, imported lazily on explicit plot
calls) adapts snapshots to the existing scientific figure builders.
"""
